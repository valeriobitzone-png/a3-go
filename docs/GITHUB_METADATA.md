# GitHub metadata

Suggested description: Go reference implementation of A3-EP with canonical envelopes and conformance tests.

Suggested topics: `go`, `epistemic-protocol`, `cloud-events`, `jcs`, `conformance`.

These are manual GitHub settings; no remote metadata is changed by this repository.